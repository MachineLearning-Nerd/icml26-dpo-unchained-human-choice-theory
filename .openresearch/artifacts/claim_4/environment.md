# Exact command and pinned environment

`uv sync --frozen && uv run --frozen python -m reproduction.run` using the repository-root `pyproject.toml`, `uv.lock`, `.python-version` (3.12), and the single repository `.venv`.
