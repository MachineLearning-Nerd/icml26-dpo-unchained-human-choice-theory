# Method

Derive the proper-loss subgradient and Fenchel identities symbolically, then substitute `x=exp(z)>0` for an all-real logistic DPO certificate. Use exact asymmetric strictly proper losses to distinguish the general identity from symmetry, and reverse the canonical-link sign as a mutation.
