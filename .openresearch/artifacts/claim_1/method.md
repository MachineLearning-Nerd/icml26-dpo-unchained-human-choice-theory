# Method

Instantiate the universal quantifiers with `psi(z)=z` and `F=sigmoid(z)`. Combine the all-`z` decomposition with endpoint properness at true probability zero. Certify the contradiction symbolically, by SMT, and with high-precision Decimal witnesses. The control reverses only the properness inequality; a Brier composite is the positive control.
